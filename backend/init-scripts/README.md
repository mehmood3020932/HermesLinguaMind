# Database initialization

Place optional PostgreSQL initialization scripts in this directory. The application should remain responsible for repeatable schema migrations; this directory is intended for bootstrap-only SQL that must run when a fresh PostgreSQL data volume is first created.
