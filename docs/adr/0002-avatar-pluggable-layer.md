# ADR-0002: Avatar as a pluggable presentation layer

## Status
Accepted

## Decision
Tutoring logic must not depend on a specific avatar engine. CPU/static companions remain a valid fallback; real inference is attached through an adapter.

## Consequences
Avatar technology can evolve without rewriting learning, memory or lesson services.
