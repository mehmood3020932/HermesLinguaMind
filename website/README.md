# Hermes Landing Page

This is a dependency-free static landing page. Replace `YOUR_GITHUB_ORG` links before publishing.

Preview locally:

```bash
python -m http.server 8088 -d website
```

Then open `http://localhost:8088`.
